spring:
  application:
    name: basel-service-recalculate
  cloud:
    config:
      uri: http://basel-config:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user